.idea/
.bot_events/
.bot_state.json
.env-checked

.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json

# Media files — local only, not tracked
*.mp4
*.wav
*.mp3
*.ts
*.aac

# Frame sequences — too large for git
frames/
segments/
work-*/
.cleaned

# BGM library — local disk only
bgm/
test/

.bili-cookie

# Backup files — local only, not tracked
*.bak

node_modules
