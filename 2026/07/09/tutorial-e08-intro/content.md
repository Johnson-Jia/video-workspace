# content.md — E08 intro（方法论：推广度量 + 多 Agent 并行 + 三层识别 AI 代码占比）

## 段落定位

E08 教程合集第 3 段（intro），方法论预告：把"多 Agent 提效"从单点放大成规模，再用"AI 代码占比度量"用数据证伪（不是嘴说提效，是数据说真提效）。预告三层识别（Co-authored-by 初筛 / 风格学复核 / 注册表组合）+ 学习目标（多 Agent 不炸、AI 占比不被骗、事前估算 vs 事后统计配合）。

## 核心信息（照脚本）

这集讲推广度量。两件事：
1. **多 Agent 并行**：把单点提效放大成规模
2. **AI 代码占比度量**：用数据证明真提效

核心是三层识别：
- Co-authored-by 初筛
- 风格学复核
- 注册表组合

学习目标：多 Agent 怎么并行不炸、AI 代码占比怎么算才不被骗、事前估算和事后统计怎么配合。

## 视觉

两件事预告横排（多 Agent 放大 + 度量证伪）+ 三层识别预告（初筛/复核/组合）。data-reveal stagger（依次浮现）。布局 space-between 撑满画布。

## 旁白（约 35s，约 186 字）

这集讲推广度量。两件事：多 Agent 并行把单点提效放大成规模，加 AI 代码占比度量用数据证明真提效。核心是三层识别：Co-authored-by 初筛，风格学复核，注册表组合。你能学到：多 Agent 怎么并行不炸，AI 代码占比怎么算才不被骗，事前估算和事后统计怎么配合。
