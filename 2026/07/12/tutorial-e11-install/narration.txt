装到自己项目很简单。Mac Linux 用 cp 命令，把需要的 skill 复制到项目的 dot claude skills 目录，把 rule 复制到 dot claude rules 目录。Windows 用 xcopy 或 copy，命令对应。不想敲命令就直接用资源管理器拖文件夹，效果一样。装完在项目根的 CLAUDE.md 里用 at 符号引用 rule，AI 自动加载，下次干活就按你的规范来。