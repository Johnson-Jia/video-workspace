十三条团队规范按职能分五层。入口是 team-conventions 总纲，CLAUDE.md 用 at 符号引用所有 rule，AI 自动加载。分层与接口层，api-response 统一响应模型，service-dao 接口实现分离。质量稳定层，error-handling 异常处理，logging 日志，testing 测试规范。数据层，database 表设计，batch 批量代替循环。并发层，async 线程池，distributed-lock 分布式锁。工程规范层，git-branch 分支命名，naming 命名规范。五层覆盖团队开发全流程。