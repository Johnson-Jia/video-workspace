先看框架八个文件的全景。registry 注册中心，装饰器登记用例类型。assertions 软断言，失败不中断加截图加收集。base_page 测试基类，封装通用操作和通用定位。runner 数据驱动调度器，读用例按类型路由。handlers 具体业务处理器，这里只放商品管理一个示例。main 执行入口，开浏览器跑用例汇总。data 用例数据，运营在这里编步骤。site 被测页，自带极简商品管理页开箱跑。八个文件对应五个核心思想。
